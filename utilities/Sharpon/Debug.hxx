#pragma once

/// Sharpon diagnostics: see `SharponProfiler.cxx` (SolsticeV1_Profiler when exported by the engine DLL).
/// Script breakpoints / a line debugger are not in the V1 API yet; reserved for future engine hooks.
