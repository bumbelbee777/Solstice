# Example timeline range: pair with "Nested sub-range" in the main layout for long-form work.
# StartTick / EndTick use the same [start, end) convention as the nested end (exclusive) field.
[TimelineRangePreset:first_beat]
DisplayName=First minute (example)
StartTick=0
EndTick=60000
Description=If your scene is 1000 ticks/sec, this is 0-60s. Adjust ticks for your project; share this INI in repo presets/Timeline/ for a common shot list.
Author=Solstice
Tags=example, template
